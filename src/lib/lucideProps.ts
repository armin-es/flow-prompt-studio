/** Default size for icons beside text in buttons and labels. */
export const iconPropsSm = {
  size: 14,
  strokeWidth: 2,
  className: 'lucide-inline',
} as const

export const iconPropsMd = {
  size: 16,
  strokeWidth: 2,
  className: 'lucide-inline',
} as const
